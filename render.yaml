services:
  - type: web
    name: vixsrc-extractor
    runtime: python
    buildCommand: pip install -r requirements.txt && playwright install chromium && playwright install-deps chromium
    startCommand: gunicorn vidsrc_extractor1:app --bind 0.0.0.0:$PORT --timeout 120
    plan: free
